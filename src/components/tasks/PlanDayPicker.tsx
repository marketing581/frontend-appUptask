/** Un día de la semana en curso (lunes a viernes), para el menú de "mover a
 *  un día" de `SimpleTaskRow` y para saber si un vencimiento es hoy. */
export type PlanDay = { key: string, label: string, isToday: boolean }
